# scripts

| Script | Purpose |
|--------|---------|
| `bootstrap.sh` | Creates the remote state backend (`bootstrap/state-backend`) |
| `get-credentials.sh <env>` | Fetches kubeconfig for the given environment's cluster |

```bash
./scripts/bootstrap.sh
./scripts/get-credentials.sh dev
```
